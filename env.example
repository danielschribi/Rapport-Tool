# Wähle das Storage-Backend:
# local        = JSON-Dateien + Uploads auf Render-Instanz (sofort lauffähig)
# drive        = Google Drive via Service Account (schreiben/lesen)
# driveread    = Nur lesen per geteiltem Drive-Link (keine Schreib-APIs)
STORAGE_BACKEND=local

# Nur bei "drive": ID des Ordners "E-Rapport-Daten" (Shared Drive/Freigabe)
DRIVE_ROOT_FOLDER_ID=

# Nur bei "driveread": öffentlicher Ordner-Link (endet auf /view?usp=sharing oder ähnlich)
DRIVE_SHARED_LINK=

# Web-Login via Session:
SESSION_SECRET=wechselmich

# Branding
APP_TITLE=Rapport-Tool
